name: block_blast
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flame: ^1.33.0
  audioplayers: ^6.5.1
  flame_audio: ^2.11.11   # ⬅️ სწორად ჩასმული ხაზზე!

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/audios/good.mp3
    - assets/audios/error.mp3
